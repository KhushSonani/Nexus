# Workspace Rules for Nexus Project

- Always use ES modules (`import`/`export`) instead of CommonJS (`require`/`module.exports`) for all JavaScript files.
- Use named exports (`export const`) instead of default exports.
